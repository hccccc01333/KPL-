# 项目设计

## 一、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    腾讯 KPL 官方接口                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
                 ┌────────────────────────┐
                 │  src/crawler/          │
                 │  - api_client.py       │
                 │  - battle_crawler.py   │
                 │  - schedule_crawler.py │
                 └────────────────────────┘
                              ↓
                ┌──────────────────────────┐
                │   data/raw/*.json         │   ← 原始数据
                └──────────────────────────┘
                              ↓
                ┌──────────────────────────┐
                │  src/parser/             │
                │  - battle_parser.py      │
                └──────────────────────────┘
                              ↓
        ┌───────────────────────────────────────┐
        │  data/processed/                       │
        │  - battles.csv                         │
        │  - players.csv                         │
        └───────────────────────────────────────┘
                              ↓
                ┌──────────────────────────┐
                │  notebooks/01_EDA        │   ← 数据探索
                │  notebooks/02_特征工程    │
                └──────────────────────────┘
                              ↓
                ┌──────────────────────────┐
                │  data/processed/         │
                │  - features.csv          │
                └──────────────────────────┘
                              ↓
                ┌──────────────────────────┐
                │  notebooks/03_模型对比    │
                │  + src/modeling/         │
                │  - trainer.py            │
                │  - simulator.py          │
                └──────────────────────────┘
                              ↓
                ┌──────────────────────────┐
                │  models/best_model.pkl   │
                │  reports/figures/        │
                └──────────────────────────┘
                              ↓
                ┌──────────────────────────┐
                │  dashboard/app.py        │   ← Streamlit
                └──────────────────────────┘
```

---

## 二、模块职责

| 层 | 模块 | 职责 |
|---|---|---|
| 数据采集 | `src/crawler/` | 调用接口、保存原始 JSON、反爬控制、断点续爬 |
| 数据解析 | `src/parser/` | JSON → DataFrame，处理嵌套与缺失 |
| 数据探索 | `notebooks/01_EDA` | 摸数据，找洞察 |
| 特征工程 | `notebooks/02_特征工程` | 严格区分赛前/赛后特征 |
| 建模 | `notebooks/03_模型对比` + `src/modeling/` | 训练、评估、保存模型 |
| 模拟器 | `src/modeling/simulator.py` | BO7 蒙特卡洛模拟 |
| 演示 | `notebooks/04_预测演示` | 可读的业务故事 |
| 工具 | `src/utils/` | 日志、路径管理等 |

---

## 三、关键设计决策

### 决策 1：为什么不直接 SQL，而用 pandas？

**理由**：项目数据规模 100-200 场比赛，pandas 内存完全够。引入 SQLite 反而增加复杂度。
**SQL 计划**：放到 V3 阶段——把数据导入 SQLite，**用 SQL 重写部分 EDA**作为简历技能展示。

### 决策 2：为什么用 conda 而不是 venv？

**理由**：xgboost、pandas 等数据栈在 conda 上预编译好，pip 装可能要本地编译（慢且容易报错）。
特别是 Windows 系统，conda 的体验明显更好。

### 决策 3：为什么不用深度学习？

**理由**：
1. 数据规模太小（100+ 样本），XGBoost 已经接近上限
2. 简历项目要"能讲清楚每一步"，DL 黑盒不利于面试讲解
3. SHAP 可解释性配合 XGBoost 是工业界标配

### 决策 4：为什么严格区分赛前/赛后特征？

**理由**：这是修复老项目最大 bug 的关键。
- ❌ 错误做法：用比赛结束后的经济、KDA 预测胜负 → 数据泄漏
- ✅ 正确做法：只用历史数据 + BP 阶段数据 → 真正的预测

### 决策 5：BO7 模拟器为什么用蒙特卡洛？

**理由**：单场比赛的胜率给出来意义不大，**电竞观众真正关心的是"4:0 还是 4:3"**。
通过蒙特卡洛 1 万次模拟，能输出比分概率分布，跟博彩公司的赔率思路一致。

---

## 四、风险与对策

| 风险 | 对策 |
|---|---|
| 接口变更 | 保存原始 JSON，再次解析 / 重新逆向 |
| 反爬封禁 | 随机延迟 + 单 IP 节流 + 失败重试 |
| 数据规模太小 | 跨赛季合并（2024 + 2025 + 2026） |
| 模型过拟合 | 5 折 CV + 严格特征筛选 |
| 季后赛预测验证不到 | 拿 2024/2025 历史决赛做"回测" |

---

## 五、简历金句（写完后回填）

> 完成项目后，把最有亮点的 3-5 个数据 / 发现写在这，方便简历直接抄。

- [ ] 数据规模：自主爬取 X 场 KPL 比赛，构建 X 行结构化数据集
- [ ] 模型表现：XGBoost 准确率 X%（5 折 CV: X% ± X%）
- [ ] 业务洞察：发现 "X" 现象（具体洞察）
- [ ] 实战验证：2026 春季赛季后赛预测命中 X/X 场
- [ ] 技术深度：通过 SHAP 解释模型，发现 Top3 关键特征是 X / Y / Z
